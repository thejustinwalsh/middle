/**
 * @packageDocumentation
 * @module @middle/dashboard
 *
 * The dashboard: a React 19 SPA plus `Bun.serve` handlers (SSE + JSON API).
 * Stub — source lands in build-spec Phase 9.
 *
 * Public surface:
 * - None yet (stub).
 *
 * Where things live:
 * - `server.ts` — the `Bun.serve` entry (the package `main`); stub until Phase 9.
 *
 * Gotchas:
 * - None.
 *
 * claude-md: false
 */
export {};
